(
    /.*/ => @ 1#(
        /\(\d+\)/ => (
            /\D/ => ""
        )
        1( /(\d+) *\* *(\d+)/ => { $1 $2 * } )
        1( 
            /(\d+) *\+ *(\d+)/ => { $1 $2 + } 
            /(\d+) *\- *(\d+)/ => { $1 $2 - } 
        )
    )
)